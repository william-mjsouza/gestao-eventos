/**
 * Mapeamento objeto-relacional (classes *Jpa e mapeadores).
 * A ser concluída na 2ª entrega, quando o domínio for separado do JPA.
 */
package com.gestaoeventos.infraestrutura.persistencia.jpa;
